// Generated at build time by scripts/write-production-env.mjs, which reads the
// API_URL environment variable. The placeholder below is deliberately invalid:
// if a production build ever ships with it, requests fail visibly instead of
// silently pointing at the wrong host.
export const environment = { production: true, apiUrl: 'https://API_URL-NOT-CONFIGURED.invalid' };
